export const checkAndInstallUpdate = async (): Promise<boolean> => {
    // check github for latest release
    // if newer version available, download and install
    return false
}

export const installUpdate = async (): Promise<boolean> => {
    // download and install
    return false
}